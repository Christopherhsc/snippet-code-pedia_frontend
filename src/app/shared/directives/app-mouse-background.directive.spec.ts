import { AppMouseBackgroundDirective } from './app-mouse-background.directive';

describe('AppMouseBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMouseBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
